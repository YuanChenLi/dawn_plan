<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <% for(var i=0; i<articles.length; i++) {%>
    <ul>
        <li><%= articles[i].title %></li>
        <li><%= articles[i].content %></li>
        <li><%= articles[i].createTime %></li>
        <li><img src="<%= articles[i].imgUrl %>" width="100px" height="70px"/></li>
    </ul>
    <% } %>
  </body>
</html>
